diff --git a/chrome/browser/ui/views/tabs/tab.cc b/chrome/browser/ui/views/tabs/tab.cc
index daa14a2f504f2..eb4e72ddc84bc 100644
--- a/chrome/browser/ui/views/tabs/tab.cc
+++ b/chrome/browser/ui/views/tabs/tab.cc
@@ -356,6 +356,7 @@ Tab::Tab(tabs::TabHandle handle, TabSlotController* controller)
   SetID(VIEW_ID_TAB);
 
   AddChildViewRaw(title_.get());
+  title_->SetFontList(tab_style_views_->GetFontList());
 
   SetEventTargeter(std::make_unique<views::ViewTargeter>(this));
 
@@ -1062,6 +1063,7 @@ void Tab::ActiveStateChanged() {
   icon_->SetActiveState(IsActive());
   alert_indicator_button_->OnParentTabButtonColorChanged();
   alert_indicator_button_->UpdateEnabledForMuteToggle();
+  title_->SetFontList(tab_style_views_->GetFontList());
   DeprecatedLayoutImmediately();
 }
 
diff --git a/chrome/browser/ui/views/tabs/tab_style_views.cc b/chrome/browser/ui/views/tabs/tab_style_views.cc
index 3bc10a66bbd7d..8cffaeca808e9 100644
--- a/chrome/browser/ui/views/tabs/tab_style_views.cc
+++ b/chrome/browser/ui/views/tabs/tab_style_views.cc
@@ -9,6 +9,7 @@
 #include <string>
 #include <utility>
 
+#include "base/command_line.h"
 #include "base/strings/strcat.h"
 #include "base/strings/string_util.h"
 #include "base/strings/utf_string_conversions.h"
@@ -16,6 +17,8 @@
 #include "chrome/browser/ui/views/tabs/horizontal_tab_style_views.h"
 #include "chrome/browser/ui/views/tabs/vertical_tab_style_views.h"
 #include "ui/gfx/color_utils.h"
+#include "ui/views/style/typography.h"
+#include "ui/views/style/typography_provider.h"
 
 // static
 std::u16string ui::metadata::TypeConverter<TabStyle::TabColors>::ToString(
@@ -74,3 +77,14 @@ std::unique_ptr<TabStyleViews> TabStyleViews::Create(
 TabStyleViews::TabStyleViews() : tab_style_(TabStyle::Get()) {}
 
 TabStyleViews::~TabStyleViews() = default;
+
+const gfx::FontList& TabStyleViews::GetFontList() const {
+  static const bool prominent_active_tab_titles =
+      base::CommandLine::ForCurrentProcess()->HasSwitch(
+          "prominent-active-tab-titles");
+  const int context = prominent_active_tab_titles && delegate()->IsActive()
+                          ? views::style::CONTEXT_BUTTON_MD
+                          : views::style::CONTEXT_LABEL;
+  return views::TypographyProvider::Get().GetFont(context,
+                                                 views::style::STYLE_PRIMARY);
+}
diff --git a/chrome/browser/ui/views/tabs/tab_style_views.h b/chrome/browser/ui/views/tabs/tab_style_views.h
index c1d2c371fc2f4..4fbafe2d3d3d0 100644
--- a/chrome/browser/ui/views/tabs/tab_style_views.h
+++ b/chrome/browser/ui/views/tabs/tab_style_views.h
@@ -83,6 +83,7 @@ class TabStyleViewDelegate {
 };
 
 namespace gfx {
+class FontList;
 class Canvas;
 }
 
@@ -111,6 +112,9 @@ class TabStyleViews {
                          float scale,
                          const TabPathFlags& flags) const = 0;
 
+  // Returns the appropriate font for the current active state.
+  const gfx::FontList& GetFontList() const;
+
   // Paints the tab.
   virtual void PaintTab(gfx::Canvas* canvas) const = 0;
 
