diff --git a/chrome/browser/ui/tabs/BUILD.gn b/chrome/browser/ui/tabs/BUILD.gn
index 32c261f368604..f69e563efb6a7 100644
--- a/chrome/browser/ui/tabs/BUILD.gn
+++ b/chrome/browser/ui/tabs/BUILD.gn
@@ -51,7 +51,10 @@ if (!is_android) {
       "features.h",
     ]
     public_deps = [ "//base" ]
-    deps = [ "//chrome/browser/ui:ui_features" ]
+    deps = [
+      "//base:i18n",
+      "//chrome/browser/ui:ui_features",
+    ]
   }
 
   source_set("tab_menu") {
diff --git a/chrome/browser/ui/tabs/features.cc b/chrome/browser/ui/tabs/features.cc
index 483be333e73a5..7dac0c26031de 100644
--- a/chrome/browser/ui/tabs/features.cc
+++ b/chrome/browser/ui/tabs/features.cc
@@ -5,6 +5,7 @@
 #include "chrome/browser/ui/tabs/features.h"
 
 #include "base/feature_list.h"
+#include "base/i18n/rtl.h"
 #include "base/time/time.h"
 #include "chrome/browser/ui/ui_features.h"
 
@@ -26,6 +27,9 @@ BASE_FEATURE(kSplitViewTabRestore, base::FEATURE_DISABLED_BY_DEFAULT);
 
 BASE_FEATURE(kTabSearchCjkWordBoundary, base::FEATURE_DISABLED_BY_DEFAULT);
 
+BASE_FEATURE(kThoriumLeftAlignedTabStripComboButton,
+             base::FEATURE_DISABLED_BY_DEFAULT);
+
 BASE_FEATURE(kVerticalTabs, base::FEATURE_DISABLED_BY_DEFAULT);
 
 BASE_FEATURE(kVerticalTabsLaunch, base::FEATURE_DISABLED_BY_DEFAULT);
@@ -102,6 +106,14 @@ BASE_FEATURE(kBackToOpener, base::FEATURE_DISABLED_BY_DEFAULT);
 BASE_FEATURE(kMigrateEverythingMenuPinnedToTabstrip,
              base::FEATURE_DISABLED_BY_DEFAULT);
 
+bool IsTabStripComboButtonBeforeTabStrip() {
+  // Views mirror child layout in RTL. Convert Thorium's physical-side
+  // preference to the corresponding logical child position.
+  const bool physically_left =
+      base::FeatureList::IsEnabled(kThoriumLeftAlignedTabStripComboButton);
+  return physically_left != base::i18n::IsRTL();
+}
+
 bool IsVerticalTabsFeatureEnabled() {
   return base::FeatureList::IsEnabled(kVerticalTabs) ||
          base::FeatureList::IsEnabled(kVerticalTabsLaunch);
diff --git a/chrome/browser/ui/tabs/features.h b/chrome/browser/ui/tabs/features.h
index c1b051dfa8bb9..b6dd0175e88e0 100644
--- a/chrome/browser/ui/tabs/features.h
+++ b/chrome/browser/ui/tabs/features.h
@@ -33,6 +33,10 @@ BASE_DECLARE_FEATURE(kSplitViewTabRestore);
 // Enables CJK word boundary detection in Tab Search.
 BASE_DECLARE_FEATURE(kTabSearchCjkWordBoundary);
 
+// Places the horizontal tab strip combo button on the physical left. It is on
+// the physical right by default.
+BASE_DECLARE_FEATURE(kThoriumLeftAlignedTabStripComboButton);
+
 // This feature will be used for the LE rollout of Vertical Tabs. It will have
 // an earlier min version than kVerticalTabsLaunch.
 BASE_DECLARE_FEATURE(kVerticalTabs);
@@ -96,6 +100,10 @@ BASE_DECLARE_FEATURE(kTabStripUnification);
 
 BASE_DECLARE_FEATURE(kMigrateEverythingMenuPinnedToTabstrip);
 
+// Returns whether the horizontal combo button should be inserted before the
+// tab strip in logical child order.
+bool IsTabStripComboButtonBeforeTabStrip();
+
 bool IsVerticalTabsFeatureEnabled();
 
 bool IsVerticalTabsExpandOnHoverFeatureEnabled();
diff --git a/chrome/browser/ui/tabs/tab_strip_prefs.cc b/chrome/browser/ui/tabs/tab_strip_prefs.cc
index 642edd40ab259..cb92a907abb3c 100644
--- a/chrome/browser/ui/tabs/tab_strip_prefs.cc
+++ b/chrome/browser/ui/tabs/tab_strip_prefs.cc
@@ -83,7 +83,9 @@ TabSearchPosition GetTabSearchPosition(
       return TabSearchPosition::kVerticalTabstrip;
     }
   }
-  return TabSearchPosition::kLeadingHorizontalTabstrip;
+  return tabs::IsTabStripComboButtonBeforeTabStrip()
+             ? TabSearchPosition::kLeadingHorizontalTabstrip
+             : TabSearchPosition::kTrailingHorizontalTabstrip;
 }
 
 }  // namespace tabs
diff --git a/chrome/browser/ui/tabs/tab_strip_prefs.h b/chrome/browser/ui/tabs/tab_strip_prefs.h
index 010bccd89c323..9142b883efa25 100644
--- a/chrome/browser/ui/tabs/tab_strip_prefs.h
+++ b/chrome/browser/ui/tabs/tab_strip_prefs.h
@@ -16,6 +16,7 @@ namespace tabs {
 
 enum class TabSearchPosition {
   kLeadingHorizontalTabstrip,
+  kTrailingHorizontalTabstrip,
   kVerticalTabstrip,
 };
 
diff --git a/chrome/browser/ui/views/frame/horizontal_tab_strip_region_view.cc b/chrome/browser/ui/views/frame/horizontal_tab_strip_region_view.cc
index 7c991f9d6181a..d6e6b476c450f 100644
--- a/chrome/browser/ui/views/frame/horizontal_tab_strip_region_view.cc
+++ b/chrome/browser/ui/views/frame/horizontal_tab_strip_region_view.cc
@@ -21,6 +21,7 @@
 #include "chrome/browser/ui/frame/window_frame_util.h"
 #include "chrome/browser/ui/immersive/immersive_mode_controller.h"
 #include "chrome/browser/ui/layout_constants.h"
+#include "chrome/browser/ui/tabs/features.h"
 #include "chrome/browser/ui/tabs/tab_menu_model.h"
 #include "chrome/browser/ui/tabs/tab_strip_model.h"
 #include "chrome/browser/ui/tabs/tab_strip_prefs.h"
@@ -170,6 +171,8 @@ std::unique_ptr<TabStrip> CreateTabStrip(
 HorizontalTabStripRegionView::HorizontalTabStripRegionView(
     BrowserView* browser_view)
     : browser_view_(browser_view),
+      render_combo_button_before_tab_strip_(
+          tabs::IsTabStripComboButtonBeforeTabStrip()),
       action_view_controller_(std::make_unique<views::ActionViewController>()) {
   views::SetCascadingColorProviderColor(
       this, views::kCascadingBackgroundColor,
@@ -180,13 +183,17 @@ HorizontalTabStripRegionView::HorizontalTabStripRegionView(
 
   BrowserWindowInterface* const browser = browser_view->browser();
 
+  std::unique_ptr<TabStripComboButton> combo_button;
   if (browser &&
       (browser->GetType() == BrowserWindowInterface::Type::TYPE_NORMAL)) {
-    combo_button_ = AddChildView(std::make_unique<TabStripComboButton>(
-        browser, TabStripComboButton::Context::kHorizontalTabStrip));
-    combo_button_->SetProperty(views::kCrossAxisAlignmentKey,
-                               views::LayoutAlignment::kCenter);
-    combo_button_->MaybeShowIPH();
+    combo_button = std::make_unique<TabStripComboButton>(
+        browser, TabStripComboButton::Context::kHorizontalTabStrip);
+    combo_button->SetProperty(views::kCrossAxisAlignmentKey,
+                              views::LayoutAlignment::kCenter);
+  }
+
+  if (combo_button && render_combo_button_before_tab_strip_) {
+    combo_button_ = AddChildView(std::move(combo_button));
   }
 
   if (base::FeatureList::IsEnabled(features::kTabGroupsFocusing)) {
@@ -270,10 +277,20 @@ HorizontalTabStripRegionView::HorizontalTabStripRegionView(
 
   SetProperty(views::kElementIdentifierKey, kTabStripRegionElementId);
 
+  if (combo_button && !render_combo_button_before_tab_strip_) {
+    combo_button_ = AddChildView(std::move(combo_button));
+    combo_button_->SetProperty(
+        views::kMarginsKey,
+        gfx::Insets::TLBR(0, 0, 0,
+                          GetLayoutConstant(LayoutConstant::kTabStripPadding)));
+  }
   if (tab_strip_action_container) {
     tab_strip_action_container_ =
         AddChildView(std::move(tab_strip_action_container));
   }
+  if (combo_button_) {
+    combo_button_->MaybeShowIPH();
+  }
   UpdateTabStripMargin();
 }
 HorizontalTabStripRegionView::~HorizontalTabStripRegionView() {
@@ -360,9 +377,9 @@ views::View::Views HorizontalTabStripRegionView::GetChildrenInZOrder() {
   return children;
 }
 
-// The TabSearchButton need bounds that overlap the TabStripContainer, which
-// FlexLayout doesn't currently support. Because of this the TSB bounds are
-// manually calculated.
+// A leading TabStripComboButton needs bounds that overlap the tab strip, which
+// FlexLayout doesn't currently support. Its bounds are therefore calculated
+// manually.
 void HorizontalTabStripRegionView::Layout(PassKey) {
   if (!tab_strip_set_) {
     return;
@@ -379,7 +396,7 @@ void HorizontalTabStripRegionView::Layout(PassKey) {
                       GetLayoutConstant(LayoutConstant::kTabStripPadding);
   }
 
-  if (combo_button_) {
+  if (combo_button_ && render_combo_button_before_tab_strip_) {
     AdjustViewBoundsRect(combo_button_, leading_offset);
   }
 
@@ -600,7 +617,8 @@ HorizontalTabStripRegionView::GetExpandOnHoverLock(
 }
 
 bool HorizontalTabStripRegionView::HasLeadingButtons() const {
-  if (combo_button_ && combo_button_->GetVisible() &&
+  if (render_combo_button_before_tab_strip_ && combo_button_ &&
+      combo_button_->GetVisible() &&
       ((combo_button_->start_button() &&
         combo_button_->start_button()->GetVisible()) ||
        (combo_button_->end_button() &&
@@ -693,10 +711,11 @@ void HorizontalTabStripRegionView::UpdateTabStripMargin() {
         GetLayoutConstant(LayoutConstant::kTabStripPadding);
   }
 
-  if (combo_button_ && ((combo_button_->start_button() &&
-                         combo_button_->start_button()->GetVisible()) ||
-                        (combo_button_->end_button() &&
-                         combo_button_->end_button()->GetVisible()))) {
+  if (render_combo_button_before_tab_strip_ && combo_button_ &&
+      ((combo_button_->start_button() &&
+        combo_button_->start_button()->GetVisible()) ||
+       (combo_button_->end_button() &&
+        combo_button_->end_button()->GetVisible()))) {
     combo_button_->SetPaintToLayer();
     combo_button_->layer()->SetFillsBoundsOpaquely(false);
     combo_button_->SetProperty(views::kViewIgnoredByLayoutKey, true);
diff --git a/chrome/browser/ui/views/frame/horizontal_tab_strip_region_view.h b/chrome/browser/ui/views/frame/horizontal_tab_strip_region_view.h
index 774e58392efa7..64f837ce82213 100644
--- a/chrome/browser/ui/views/frame/horizontal_tab_strip_region_view.h
+++ b/chrome/browser/ui/views/frame/horizontal_tab_strip_region_view.h
@@ -138,6 +138,8 @@ class HorizontalTabStripRegionView final : public TabStripRegionView {
   raw_ptr<views::Button> new_tab_button_ = nullptr;
   raw_ptr<TabStripControlButton> unfocus_button_ = nullptr;
 
+  const bool render_combo_button_before_tab_strip_;
+
   std::unique_ptr<views::ActionViewController> action_view_controller_;
 
   const base::CallbackListSubscription subscription_ =
