diff --git a/chrome/browser/ui/tabs/tab_menu_model.cc b/chrome/browser/ui/tabs/tab_menu_model.cc
index 97c6ded1ac589..37d1f088f996b 100644
--- a/chrome/browser/ui/tabs/tab_menu_model.cc
+++ b/chrome/browser/ui/tabs/tab_menu_model.cc
@@ -340,6 +340,10 @@ void TabMenuModel::Build(int index) {
   }
   SetElementIdentifierAt(GetItemCount() - 1, kAddNewTabAdjacentMenuItem);
 
+  AddItemWithStringId(TabStripModel::CommandNewTabToLeft,
+                      base::i18n::IsRTL() ? IDS_TAB_CXMENU_NEWTABTORIGHT
+                                          : IDS_TAB_CXMENU_NEWTABTOLEFT);
+
   if (!tab_strip_->GetSplitForTab(index).has_value()) {
     if (tab_strip_->GetActiveTab()->IsSplit()) {
       swap_with_split_submenu_ =
diff --git a/chrome/browser/ui/tabs/tab_strip_model.cc b/chrome/browser/ui/tabs/tab_strip_model.cc
index 01f863a9f43a8..7937d9a4939e1 100644
--- a/chrome/browser/ui/tabs/tab_strip_model.cc
+++ b/chrome/browser/ui/tabs/tab_strip_model.cc
@@ -2595,6 +2595,7 @@ bool TabStripModel::IsContextMenuCommandEnabled(
   }
 
   switch (command_id) {
+    case CommandNewTabToLeft:
     case CommandNewTabToRight:
     case CommandCloseTab:
       return true;
@@ -2703,6 +2704,15 @@ void TabStripModel::ExecuteContextMenuCommand(int context_index,
     return;
   }
   switch (command_id) {
+    case CommandNewTabToLeft: {
+      base::RecordAction(UserMetricsAction("TabContextMenu_NewTab"));
+      UMA_HISTOGRAM_ENUMERATION("Tab.NewTab", NewTabTypes::kNewTabContextMenu,
+                                NewTabTypes::kNewTabEnumCount);
+      delegate()->AddTabAt(GURL(), context_index, true,
+                           GetTabGroupForTab(context_index));
+      break;
+    }
+
     case CommandNewTabToRight: {
       base::UmaHistogramCounts1000(
           "Tab.ContextMenu.NewTabToRight.SelectedTabsCount",
diff --git a/components/tabs/public/tab_context_menu_command.h b/components/tabs/public/tab_context_menu_command.h
index 0ed448fdc4af7..684e8434e93a4 100644
--- a/components/tabs/public/tab_context_menu_command.h
+++ b/components/tabs/public/tab_context_menu_command.h
@@ -39,6 +39,7 @@ enum TabContextMenuCommand {
   CommandGlicSwitchToRecentConversation,
   CommandGlicUnshare,
   CommandToggleFocusGroup,
+  CommandNewTabToLeft,
 };
 // LINT.ThenChange(//tools/metrics/histograms/metadata/tab/histograms.xml:TabContextMenuCommand)
 
diff --git a/tools/metrics/histograms/metadata/tab/histograms.xml b/tools/metrics/histograms/metadata/tab/histograms.xml
index 191c3eba1a610..7b26bf64c462d 100644
--- a/tools/metrics/histograms/metadata/tab/histograms.xml
+++ b/tools/metrics/histograms/metadata/tab/histograms.xml
@@ -93,6 +93,7 @@ chromium-metrics-reviews@google.com.
   <variant name="MoveToExistingWindow"
       summary="the Move to existing window command"/>
   <variant name="MoveToNewWindow" summary="the Move to new window command"/>
+  <variant name="NewTabToLeft" summary="the New Tab To Left command"/>
   <variant name="NewTabToRight" summary="the New Tab To Right command"/>
   <variant name="Reload" summary="the Reload command"/>
   <variant name="RemoveFromGroup" summary="the Remove from Group command"/>
