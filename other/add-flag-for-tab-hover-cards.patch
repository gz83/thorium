diff --git a/chrome/browser/ui/views/tabs/common/tab_group_header_view.cc b/chrome/browser/ui/views/tabs/common/tab_group_header_view.cc
index c7c218422c023..7433d1d63532d 100644
--- a/chrome/browser/ui/views/tabs/common/tab_group_header_view.cc
+++ b/chrome/browser/ui/views/tabs/common/tab_group_header_view.cc
@@ -4,6 +4,7 @@
 
 #include "chrome/browser/ui/views/tabs/common/tab_group_header_view.h"
 
+#include "base/command_line.h"
 #include "base/task/single_thread_task_runner.h"
 #include "build/build_config.h"
 #include "build/buildflag.h"
@@ -583,7 +584,10 @@ void TabGroupHeaderView::OnDataChanged(
   UpdateIsCollapsed();
   UpdateAccessibleName();
 
-  if (features::IsTabGroupHoverCardsEnabled()) {
+  const bool tab_hover_cards_tooltip =
+      base::CommandLine::ForCurrentProcess()->GetSwitchValueASCII(
+          "tab-hover-cards") == "tooltip";
+  if (features::IsTabGroupHoverCardsEnabled() && !tab_hover_cards_tooltip) {
     SetHoverCardDataFrom(tab_group_data);
   } else {
     UpdateTooltipText();
diff --git a/chrome/browser/ui/views/tabs/common/tab_view.cc b/chrome/browser/ui/views/tabs/common/tab_view.cc
index 03bb87258a1d2..24ea6867e4fc0 100644
--- a/chrome/browser/ui/views/tabs/common/tab_view.cc
+++ b/chrome/browser/ui/views/tabs/common/tab_view.cc
@@ -54,6 +54,7 @@
 #include "chrome/browser/ui/views/tabs/tab/alert_indicator_button.h"
 #include "chrome/browser/ui/views/tabs/tab/glow_hover_controller.h"
 #include "chrome/browser/ui/views/tabs/tab/tab_accessibility.h"
+#include "chrome/browser/ui/views/tabs/tab.h"
 #include "chrome/browser/ui/views/tabs/tab/tab_close_button.h"
 #include "chrome/browser/ui/views/tabs/tab/tab_icon.h"
 #include "chrome/browser/ui/views/tabs/tab/tab_title.h"
@@ -858,6 +859,16 @@ void TabView::PaintChildren(const views::PaintInfo& info) {
   View::PaintChildren(info);
 }
 
+std::u16string TabView::GetRenderedTooltipText(const gfx::Point& p) const {
+  const bool tab_hover_cards_tooltip =
+      base::CommandLine::ForCurrentProcess()->GetSwitchValueASCII(
+          "tab-hover-cards") == "tooltip";
+  if (tab_hover_cards_tooltip) {
+    return Tab::GetTooltipText(tab_data_.title, tab_data_.alert_state);
+  }
+  return std::u16string();
+}
+
 void TabView::OnPaint(gfx::Canvas* canvas) {
   // Split pinned tabs have a merged background that is rendered in
   // `SplitTabView`.
diff --git a/chrome/browser/ui/views/tabs/common/tab_view.h b/chrome/browser/ui/views/tabs/common/tab_view.h
index 1a27579c523a1..19ca7967a4aef 100644
--- a/chrome/browser/ui/views/tabs/common/tab_view.h
+++ b/chrome/browser/ui/views/tabs/common/tab_view.h
@@ -7,6 +7,7 @@
 
 #include <memory>
 #include <optional>
+#include <string>
 #include <vector>
 
 #include "base/callback_list.h"
@@ -26,6 +27,7 @@
 #include "third_party/skia/include/core/SkScalar.h"
 #include "ui/base/metadata/metadata_header_macros.h"
 #include "ui/gfx/canvas.h"
+#include "ui/gfx/geometry/point.h"
 #include "ui/views/context_menu_controller.h"
 #include "ui/views/layout/layout_manager_base.h"
 #include "ui/views/masked_targeter_delegate.h"
@@ -142,6 +144,7 @@ class TabView : public views::View,
   void OnMouseExited(const ui::MouseEvent& event) override;
   bool OnMouseDragged(const ui::MouseEvent& event) override;
   void OnGestureEvent(ui::GestureEvent* event) override;
+  std::u16string GetRenderedTooltipText(const gfx::Point& p) const override;
   void PaintChildren(const views::PaintInfo& info) override;
   void OnPaint(gfx::Canvas* canvas) override;
   void AddedToWidget() override;
diff --git a/chrome/browser/ui/views/tabs/hovercard/tab_hover_card_controller.cc b/chrome/browser/ui/views/tabs/hovercard/tab_hover_card_controller.cc
index e9b830d6bc64b..a10a64ea4d428 100644
--- a/chrome/browser/ui/views/tabs/hovercard/tab_hover_card_controller.cc
+++ b/chrome/browser/ui/views/tabs/hovercard/tab_hover_card_controller.cc
@@ -299,6 +299,10 @@ bool TabHoverCardController::IsHoverCardShowingForTab(
 void TabHoverCardController::UpdateHoverCard(
     HoverCardAnchorTarget* anchor_target,
     TabSlotController::HoverCardUpdateType update_type) {
+  if (base::CommandLine::ForCurrentProcess()->HasSwitch("tab-hover-cards")) {
+    return;
+  }
+
   if (hide_hover_card_lock_count_ > 0) {
     anchor_target = nullptr;
     update_type = TabSlotController::HoverCardUpdateType::kAnimating;
diff --git a/chrome/browser/ui/views/tabs/tab.cc b/chrome/browser/ui/views/tabs/tab.cc
index 55ab248666cd5..fbdd313f1d232 100644
--- a/chrome/browser/ui/views/tabs/tab.cc
+++ b/chrome/browser/ui/views/tabs/tab.cc
@@ -1311,6 +1311,16 @@ float Tab::GetZValue() const {
   return sort_value;
 }
 
+std::u16string Tab::GetRenderedTooltipText(const gfx::Point& p) const {
+  const bool tab_hover_cards_tooltip =
+      base::CommandLine::ForCurrentProcess()->GetSwitchValueASCII(
+          "tab-hover-cards") == "tooltip";
+  if (tab_hover_cards_tooltip) {
+    return GetTooltipText(data_.title, data_.alert_state);
+  }
+  return std::u16string();
+}
+
 // static
 std::u16string Tab::GetTooltipText(const std::u16string& title,
                                    std::optional<tabs::TabAlert> alert_state) {
diff --git a/chrome/browser/ui/views/tabs/tab.h b/chrome/browser/ui/views/tabs/tab.h
index a89cd64eeda5a..e2980da5d451b 100644
--- a/chrome/browser/ui/views/tabs/tab.h
+++ b/chrome/browser/ui/views/tabs/tab.h
@@ -110,6 +110,7 @@ class Tab : public gfx::AnimationDelegate,
   void OnMouseEntered(const ui::MouseEvent& event) override;
   void OnMouseExited(const ui::MouseEvent& event) override;
   void OnGestureEvent(ui::GestureEvent* event) override;
+  std::u16string GetRenderedTooltipText(const gfx::Point& p) const override;
   gfx::Size CalculatePreferredSize(
       const views::SizeBounds& available_size) const override;
   void PaintChildren(const views::PaintInfo& info) override;
